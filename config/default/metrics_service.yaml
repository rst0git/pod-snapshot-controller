apiVersion: v1
kind: Service
metadata:
  labels:
    control-plane: pod-snapshot-controller
    app.kubernetes.io/name: pod-snapshot-controller
    app.kubernetes.io/managed-by: kustomize
  name: pod-snapshot-controller-metrics-service
  namespace: system
spec:
  ports:
  - name: https
    port: 8443
    protocol: TCP
    targetPort: 8443
  selector:
    control-plane: pod-snapshot-controller
    app.kubernetes.io/name: pod-snapshot-controller
